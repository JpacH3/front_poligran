services:
  - type: web
    name: odonto-smile
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        value: tu_clave_secreta_compleja
      - key: DATABASE_URL
        value: sqlite:///instance/usuarios.db